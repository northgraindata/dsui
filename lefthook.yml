pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{js,jsx,ts,tsx,json,yml,yaml}"
      exclude: "(^|/)\\."
      run: bunx biome check --write {staged_files}
    typecheck:
      run: bun run typecheck
