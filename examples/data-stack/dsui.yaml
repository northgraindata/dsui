adapters:
  airflow:
    package: "@northgraindata/dsui-adapter-airflow"
  # Local workspace package, loaded in-process through the uniform loader.
  duckdb:
    package: "@northgraindata/dsui-adapter-duckdb"

services:
  - id: analytics
    adapter: duckdb
    name: Analytics database
    connection:
      method: memory
