name: dsui_core_v1_fixture
version: "1.0.0"
config-version: 2

profile: dsui_core_v1_fixture

model-paths: [models]
seed-paths: [seeds]
test-paths: [tests]
macro-paths: [macros]
docs-paths: [docs]

models:
  dsui_core_v1_fixture:
    +materialized: view
    staging:
      +schema: staging
    marts:
      +schema: marts

seeds:
  dsui_core_v1_fixture:
    +schema: raw
