name: dsui_fusion_v2_fixture
version: "2.0.0"
config-version: 2

profile: dsui_fusion_v2_fixture

model-paths: [models]
seed-paths: [seeds]
test-paths: [tests]
macro-paths: [macros]
docs-paths: [docs]

models:
  dsui_fusion_v2_fixture:
    +materialized: view
    staging:
      +schema: staging
    marts:
      +schema: marts

seeds:
  dsui_fusion_v2_fixture:
    +schema: raw
